# Local + per-environment env files
.env
.env.local
.env.preview
.env.prod
# Allow templates only
!.env.example
!.env.preview.example
!.env.prod.example

node_modules/
dist/
*.log

# User-private generated files (resume PDFs, auto-apply screenshots)
storage/
# Legacy locations — keep ignored in case they linger
public/resumes/
public/autoapply/
